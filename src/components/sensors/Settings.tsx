import React from "react";
import styled from "styled-components";

type Props = {};

const Settings = (props: Props) => {
  return <Container>SettingSensor works</Container>;
};
export default Settings;

const Container = styled.div`
  color: #fff;
`;
