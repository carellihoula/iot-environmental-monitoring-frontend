import React from "react";
import styled from "styled-components";

type Props = {};

const SensorSelection = (props: Props) => {
  return <Container>Selection works</Container>;
};

export default SensorSelection;

const Container = styled.div`
  color: #fff;
`;
